[project]
name = "test-protocol-ext"
version = "0.1.0"
description = "Provides protocol for XPC tests"
requires-python = ">=3.10"
dependencies = []
classifiers = ["Private :: Do Not Upload"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
